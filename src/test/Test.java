package test;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import sn.senforage.dao.IUser;
import sn.senforage.dao.UserImpl;
import sn.senforage.entities.Client;
import sn.senforage.entities.User;
import sn.senforage.entities.Village;


public class Test {
	
	public static void main(String[] args) {
		
		/*
		 * EntityManagerFactory emf =
		 * Persistence.createEntityManagerFactory("senforage"); EntityManager em =
		 * emf.createEntityManager(); IUser userdao = new UserImpl();
		 */
		  
		  
		 // User user = new User(); user.setNom("sow"); user.setPrenom("momo");
		  //user.setEmail("momo@gmail.com"); user.setPassword("");
		  
		  //Village vl = new Village();
		  
		/*
		 * Client cl = new Client(); cl.setAdresse("fghjk"); cl.setNomFamille("dfghj");
		 * cl.setTelephone("dfghj"); cl.setUser(em.find(User.class, 1));
		 * cl.setVillage(em.find(Village.class, 1));
		 * 
		 * em.getTransaction().begin(); em.persist(cl); em.getTransaction().commit();
		 */
			
		}

}
